# v1 — the previous site, frozen

The site exactly as it stood at commit 4af9aef (31 July 2026), kept live at
/v1/ so the client can compare and so we can fall back without a deploy. Two
changes only: a noindex robots meta, and the canonical/og:url tags removed, so
this copy never competes with the live site in search.

Do not edit anything in here.
